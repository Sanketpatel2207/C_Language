#include <stdio.h>
int main(){
    int n,i;
    printf("Numbers divisible by 5 are :\n");
    for (i = 0; i < 100; i++)
    {
        if (i%5==0)
        {
            printf("%d\n",i);
        }
        
    }
 return 0;   
}
